🎯 Goal

Update the existing Access Gateway Page so that:

The shared password remains exactly as it is (unchanged and editable by Giovanni only).

The access code field accepts any value in the format INV-LAB-<number> where <number> ranges from 1 to 200.

All codes in that range grant access (no need to list them manually).

Any invalid or out-of-range code should show a clear inline error message in the existing UI style.

Do not change the current visual design, layout, or page structure — only the validation logic.

⚙️ Implementation Steps

Open /access/script.js (or whichever script handles validation for the login form).

Locate the section that validates the password and access code input.

Replace the existing access code validation logic with this:

function checkAccess() {
  const password = document.getElementById("password").value.trim();
  const code = document.getElementById("code").value.trim().toUpperCase();

  const correctPassword = "advancedlab2025"; // keep this or use the existing password variable

  // ✅ Step 1: Validate password
  if (password !== correctPassword) {
    showError("Invalid password. Please try again.");
    return;
  }

  // ✅ Step 2: Validate access code (must match INV-LAB-<1–200>)
  const regex = /^INV-LAB-(\d{1,3})$/;
  const match = code.match(regex);

  if (!match) {
    showError("Invalid access code format. Use INV-LAB-1 to INV-LAB-200.");
    return;
  }

  const codeNumber = parseInt(match[1]);
  if (codeNumber < 1 || codeNumber > 200) {
    showError("Access code must be between INV-LAB-1 and INV-LAB-200.");
    return;
  }

  // ✅ Success — Access granted
  window.location.href = "/app/dashboard";
}

// Helper function for inline error display (use your current style)
function showError(message) {
  const errorDiv = document.getElementById("error-message");
  if (errorDiv) {
    errorDiv.innerText = message;
    errorDiv.style.display = "block";
  } else {
    alert(message);
  }
}


Make sure there’s a <div id="error-message"></div> below your “Enter Lab” button in index.html (if not already there).

Keep everything else (password input, design, redirect, etc.) unchanged.

✅ Acceptance Criteria

Password validation continues to work as before.

Access codes INV-LAB-1 through INV-LAB-200 all grant access.

Invalid codes or formats show an inline red error message (no alerts).

Layout and styling remain identical to your current version.

Only Giovanni can modify the password in code — users cannot.

🧠 Summary

This update ensures that:

Every valid user code (INV-LAB-1 → INV-LAB-200) works automatically.

You can personalize which code you send to each student.

You don’t have to manually store or check each code.

The overall login experience and design stay exactly the same.